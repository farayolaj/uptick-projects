describe("createEvent", () => {
  it("should create a new event in the relational database", async () => {
    // const eventData = {
    //   title: "message",
    //   data: "This is a message.",
    //   roomId: 1,
    //   userId: 2,
    // };
    // await eventModel.createEvent(eventData);
  });
});

describe("getEventsInRoom", () => {
  it("should return all of the events in a room", async () => {
    // const roomId = 1;
    // await eventModel.getEventsInRoom({ roomId });
  });
});
